import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.paula.mercavis',
  appName: 'Mercavis',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
